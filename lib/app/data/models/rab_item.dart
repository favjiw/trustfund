/// A single line in a campaign's budget plan (Rencana Anggaran Biaya).
class RabItem {
  final String name;
  final String estimate;

  const RabItem({required this.name, required this.estimate});
}
