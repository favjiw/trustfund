/// A preset donation amount shown in the nominal picker grid.
class NominalOption {
  final String label;
  final String value;
  final int amount;

  const NominalOption({
    required this.label,
    required this.value,
    required this.amount,
  });
}
